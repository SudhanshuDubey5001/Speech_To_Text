<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="speech.my.com.cloudspeechtotext">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".other.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/fb_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fb_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.FacebookActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DashboardActivity" />
        <activity android:name=".activity.OutputActivity" />
        <activity android:name=".activity.LogActivity" />
        <activity android:name=".activity.SampleActivity"></activity>
    </application>

</manifest>